[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install", "cd backend && npm install"]

[phases.build]
cmds = ["echo 'Building project...'"]

[start]
cmd = "cd backend && node server.js"
